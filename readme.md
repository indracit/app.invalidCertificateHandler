# middleware.invalidCertificateHandler

## Setup

    2. create config.json file with below format inside root folder
                {
            "port" : "",
            "baseUrl" : "",
                }

    3.  npm i
    4.  npm start